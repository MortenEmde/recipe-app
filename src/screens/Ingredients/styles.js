import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  contentContainer: {
    height: '100%',
    backgroundColor: '#fff',
    alignItems: 'center',
    paddingTop: 10,
  },
});

export default styles;
